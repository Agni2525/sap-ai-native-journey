I'm an ECC ABAP developer becoming a cloud-native, AI-native SAP developer. This repo is AssetPulse — a maintenance request intake and triage system — and it's the one product I'm building across eleven months, one increment per month: a local CAP backend, then real HANA Cloud, then a Fiori UI, then live S/4 data instead of mocks, then integration events, then RAG over equipment manuals, then an agent that runs the whole triage chain conversationally. Every commit in this repo is evidence for an interview at SAP Labs India, and eventually Walldorf. If I'm reading this after a gap, the fix is: check the last commit message, check journal.md, and pick up from there — not restart.